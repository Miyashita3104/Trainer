<%= form_with(model: trainer_knowledge, local: true) do |form| %>
  <% if trainer_knowledge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trainer_knowledge.errors.count, "error") %> prohibited this trainer_knowledge from being saved:</h2>

      <ul>
      <% trainer_knowledge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :script %>
    <%= form.text_field :script %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
